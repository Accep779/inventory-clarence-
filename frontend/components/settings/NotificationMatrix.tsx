import React from 'react';

export default function NotificationMatrix() {
  return (
    <div className="space-y-6">
      <h2 className="text-2xl font-bold text-white">Notification Matrix</h2>
      <p className="text-slate-400">Configure which events trigger emails, Slack messages, or in‑app alerts.</p>
      {/* Add toggles, channels list, etc. */}
    </div>
  );
}
